import botErrors from '../interfaces/bot.errors';

const botErrors: botErrors = {
  errorWhileExecutingCommand:
    `An error occured while executing the command`,
  conversionErrorOccured: 'A conversion error occured',
};

export default botErrors;
